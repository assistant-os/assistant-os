import Middleware from '../os/middleware'

let info = new Middleware()

// let cache = {}
//
// info.hear('remember new private information', (req, res) => {
//     req.reply('Ok now give me ')
// })

export default info
